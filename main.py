from doctest import OPTIONFLAGS_BY_NAME
import pyautogui
import keyboard
import time
import random
import modes.BypassesForDiscord
import socket
import tkinter as t
import modes.BypassesForMC
import customtkinter as tk
def bypass42():
    print("DO NOT USE THIS FOR CYBER BULLYING!!!!!!!!!!!!!!!!!!!!")
    time.sleep(1)
    print("Who Do You Want To @? (type no if nobody)")
    name = input('>> ')
    print("Please Enter Chat")
    print("starting in 5")
    time.sleep(1)
    print("starting in 4")
    time.sleep(1)
    print("starting in 3")
    time.sleep(1)
    print("starting in 2")
    time.sleep(1)
    print("starting in 1")
    time.sleep(1)
    if name == 'no':
        name_enabled = False
    else:
        name_enabled = True
    while True:
        words = 'die', 'Ez', 'FAG', "NoLife", "BOT", "NPC"
        Message = random.choice(words)
        if name_enabled == True:
            keyboard.write('@')
           
            keyboard.write(name)
            keyboard.write(' ')
        keyboard.write(Message)
        pyautogui.press("ENTER")
        time.sleep(1)
def bypass32():
    time.sleep(5)
    ip = 'www.discord.gg'
    port = int(25565)
    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    s.connect((ip, port))
    print("Please Enter Chat")
    time.sleep(5)
 
    while True:
        options = 'l3hu42lkj', 'kj4h5JH', "fkalsjdfkl", "flkadsjf", "OFJdf90pufIU.", '*f8s9J*(', '*jfialhfaHLKJ', ':LFHiaaaaohu7', 'LIUfhialudfhai7fffudl', '*aaudfnaosdfjaoid', '&oq73648&^lhaoisu', ':IHo3infiiiioahf', 'IOniuoajoud', 'IOjfopsiujpo3', 'oi&Nudjoijwfp8a', 'op23iu4ijdaisopfjas', 'po28yhlofuuuuipayOIYHuifhifHhUDFLudflkjdhialhLIUFHliud', 'OFNdjahfopUUIfashf;OHPOEj', 'adksfja;lkdsjflkasdjfaklsdjflk', '73y2oih&*649308uy(*&^43iujhdo;98', 'OKAJDSFA;L8uOupuiou0p9uOIUPOU.', 'kjaf*&(^%^&$%^&*(&&)*(^&(*^^&%^*&%*&^%*&)(*&()*&*'
        bypass = random.choice(options)
        keyboard.write(Message)
        keyboard.write(" [{[")
        keyboard.write(bypass)
        keyboard.write("}]} ")
        pyautogui.press("ENTER")
        time.sleep(1.1)
def bypass22():
    time.sleep(5)
    ip = 'www.discord.gg'
    port = int(25565)

    
    print("Please Enter Chat")
    time.sleep(5)
    while True:
        options = 'l3hu42lkj', 'kj4h5JH', '*f8s9J*(', '*jfialhfaHLKJ', ':LFHiaaaaohu7', 'LIUfhialudfhai7fffudl', '*aaudfnaosdfjaoid', '&oq73648&^lhaoisu', ':IHo3infiiiioahf', 'IOniuoajoud', 'IOjfopsiujpo3', 'oi&Nudjoijwfp8a', 'op23iu4ijdaisopfjas', 'po28yhlofuuuuipayOIYHuifhifHhUDFLudflkjdhialhLIUFHliud', 'OFNdjahfopUUIfashf;OHPOEj', 'adksfja;lkdsjflkasdjfaklsdjflk', '73y2oih&*649308uy(*&^43iujhdo;98', 'OKAJDSFA;L8uOupuiou0p9uOIUPOU.', 'kjaf*&(^%^&$%^&*(&&)*(^&(*^^&%^*&%*&^%*&)(*&()*&*'
        bypass = random.choice(options)
        keyboard.write(Message)
        keyboard.write(" [{[")
        keyboard.write(bypass)
        keyboard.write("}]} ")
        pyautogui.press("ENTER")
        time.sleep(0.9)
def bypass12():
    time.sleep(5)
    
    print("Please Enter Chat")
    time.sleep(5)
    while True:
        bypass1 = 'qwertyuiopasdfghjklzxcvbnm'
        import random
        options = 'alksdjf', 'alsdjf', 'aljsdh', 'dfhjgb', 'tedhj', 'vjghbn', 'rytjhk','tgvjhb', 'afsd','asdfasdf','adfad'
        bypass = random.choice(options)
        keyboard.write(Message)
        keyboard.write(" [")
        keyboard.write(bypass)
        keyboard.write("] ")
        pyautogui.press("ENTER")

        time.sleep(0.8)
def normal2():
    time.sleep(5)
        
    print("Please Enter Chat")
    time.sleep(5)
    while True:
        time.sleep(0.5)
        keyboard.write(Message)
        pyautogui.press("ENTER")
def nobypass2():
    time.sleep(1)
    
    print("Please Enter Chat")
    time.sleep(5)
    while True:
        keyboard.write(Message)
        pyautogui.press("ENTER") 
def UACBypass2():

    
    time.sleep(1)
    print("Please Enter Chat!")
    time.sleep(5)
    print("Started!")
    countdown = 5
    while True:
        bypass = random.choice(modes.BypassesForMC.bypasses)

        if countdown > 0:
            keyboard.write(Message)
            keyboard.write(" [")
            keyboard.write(bypass)
            keyboard.write("] ")
            pyautogui.press("ENTER")
            countdown -= 1
        else:
            time.sleep(6)
def ParadoxRIP2():
  
    
    time.sleep(1)
    print("Please Enter Chat! :)")
    time.sleep(5)
    while True:
        bypass = random.choice(modes.BypassesForMC.bypasses)
        keyboard.write(Message)
        keyboard.write(" [")
        keyboard.write(bypass)
        keyboard.write("] ")
        pyautogui.press("ENTER")
        pause += 1
        if pause < 5:
            pause = 0
            time.sleep(1)
def RubedoIsTrash2():
    
   
    time.sleep(1)
    print("Please Enter Chat :)")
    time.sleep(5)
    while True:
        bypass = random.choice(modes.BypassesForMC.bypasses)
        keyboard.write(Message)
        keyboard.write(" [")
        keyboard.write(bypass)
        keyboard.write("] ")
        pyautogui.press("ENTER")
        time.sleep(0.00000000001)
def RadonIsHardToBypass2():
    
   
    time.sleep(1)
    print("Please Enter Chat :)")
    time.sleep(5)
    while True:
        bypass = random.choice(modes.BypassesForMC.bypasses)
        keyboard.write(Message)
        keyboard.write(" [")
        keyboard.write(bypass)
        keyboard.write("] ")
        pyautogui.press("ENTER")
        time.sleep(0.4)
        print("Bypassing")
        #attempts to use advanced bypassing techniques 
        ip = 'play.nethergames.org'
        port = int(19132)
        s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        s.connect((ip, port))
def MineplexIsGay2():
 
    
    time.sleep(1)
    print("Please Enter Chat :)")
    time.sleep(5)
    while True:
        
        bypasstime = random.choice(modes.BypassesForMC.bypasstimes)
        bypass = random.choice(modes.BypassesForMC.bypasses)
        keyboard.write(Message)
        keyboard.write(" [")
        keyboard.write(bypass)
        keyboard.write("] ")
        pyautogui.press("ENTER")
        time.sleep(bypasstime)
def PayForYourGames2():
    print("PAY FOR YOUR GAMES!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!")
   
    
    time.sleep(1)
    print("Please Enter Chat :)")
    time.sleep(5)
    while True:
        bypass = random.choice(modes.BypassesForMC.bypasses)
        keyboard.write(Message)
        keyboard.write(" [")
        keyboard.write(bypass)
        keyboard.write("] ")
        pyautogui.press("ENTER")
def FlareonIsTooGood2():
    print(
        """
        This is a WIP bypass
        Honestly i just cant be bothered to code it,
        so if u wanna try the beta bypass, the code you need to type in the console is:
        FlareonBypass123
        """
    )   
def FlareonBeta2():
   
    time.sleep(1)
    print("Please Enter Chat :)")
    time.sleep(5)
    while True:
        ip = 'play.hivemc.com'
        port = int(19132)
        s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        s.connect((ip, port))   
        bypass = random.choice(modes.BypassesForMC.bypasses)
        keyboard.write(Message)
        keyboard.write(" [")
        keyboard.write(bypass)
        keyboard.write("] ")
        time.sleep(0.0001)
        pyautogui.press("ENTER")
        time.sleep(1)
        s.close((ip, port))

def ldmode():


    ldmodegui = tk.CTk()
    ldmodegui.title("Appearance Settings")
    ldmodegui.geometry("300x400")

    darkmode = tk.CTkButton(master=ldmodegui, text='Dark Mode', command=darkMode)
    darkmode.pack(pady=25, padx=40, fill="both", expand=True)


    notdarkmode = tk.CTkButton(master=ldmodegui, text='Light Mode', command=NotdarkMode)
    notdarkmode.pack(pady=25, padx=40, fill="both", expand=True)

    #accent_green = tk.CTkButton(master=ldmodegui, text='Green', command=green_mode)
    #accent_green.pack(pady=25, padx=40, fill="both", expand=True)


    ldmodegui.mainloop()
def darkMode():
    tk.set_appearance_mode("dark")
def NotdarkMode():

    tk.set_appearance_mode("light")

print("What platform are you going to be using this on?")
print(
    """
    - Discord
    - Minecraft
    """
)
question = input(">>")
if question == 'Discord':
    print(
        """
        Welcome!
        There are 7 modes
        - No Bypass (all out)
        - Normal (normal bypass, doesnt really bypass anything)
        - bypass1 (small bypass)
        - bypass2 (big bypass)
        - bypass3 (neearly 100% will bypass)
        - antiredard (shouts at ppl)
        - clickgui (WIP)
        """
    )
    print("Please Choose one")
    ans = input(">>")
    while True:
        if ans == 'Normal':
            modes.BypassesForDiscord.normal()

        elif ans == 'bypass1':
            modes.BypassesForDiscord.bypass1()   
        elif ans == 'bypass2':
            modes.BypassesForDiscord.bypass2()
        elif ans == 'bypass3':
            modes.BypassesForDiscord.bypass3()
        elif ans == 'antiretard':
            modes.BypassesForDiscord.bypass4()
        elif ans == 'No Bypass':
            modes.BypassesForDiscord.nobypass()
        elif ans == 'clickgui':
            #setting everything up

            print("\nWhat Do You Want Your Message To Be?")
            Message = input(">>")
            window = tk.CTk()
            window.title('ClickGUI') 
            tk.set_default_color_theme("green")
            window.geometry("400x650")

            frame_1 = tk.CTkFrame(master=window)
            frame_1.pack(pady=25, padx=40, fill="both", expand=True)

            #the guts of it
            nobypass2 = tk.CTkButton(master=frame_1, text='No Bypass', command=nobypass2)
            
            normal2 = tk.CTkButton(master=frame_1, text='Normal', command=normal2)

            bypass12 = tk.CTkButton(master=frame_1, text='Bypass.1', command=bypass12)

            bypass22 = tk.CTkButton(master=frame_1, text='Bypass.2', command=bypass22)

            bypass32 = tk.CTkButton(master=frame_1, text='Bypass.3', command=bypass32)

            bypass42 = tk.CTkButton(master=frame_1, text='Anti-Retard',command=bypass42)

            #.pack displays everything on screen
            nobypass2.pack(pady=25, padx=40, fill="both", expand=True)

            normal2.pack(pady=25, padx=40, fill="both", expand=True)

            bypass12.pack(pady=25, padx=40, fill="both", expand=True)

            bypass22.pack(pady=25, padx=40, fill="both", expand=True)

            bypass32.pack(pady=25, padx=40, fill="both", expand=True)

            bypass42.pack(pady=25, padx=40, fill="both", expand=True)


            frame_2 = tk.CTkFrame(master=window)
            frame_2.pack(pady=25, padx=40, fill="both", expand=True)

            label_1 = tk.CTkButton(master=frame_2, text='Appearance Settings', command=ldmode)
            label_1.pack(pady=25, padx=40, fill="both", expand=True)

            


            window.mainloop()
        else:
            print("Unknown Function")
            break
elif question == 'Minecraft':
    print("Welcome. This is designed for Minecraft Bedrock or Education Edition but can be used on Java edition!")
    print(
        """
        Modes:
        - NoBypass = WILL NOT BYPASS ANTI-SPAM (if it does tell them their Anti-Spam is trash)
        - Normal = Bypasses shitty Anti-spams
        - UAC = Unity Anti cheat Anti-Spam bypass
        - Paradox = Paradox Anti-Spam bypass (RIP Paradox)
        - Rubedo = Rubedo Anti-Spam bypass (WIP)
        - Radon = Radon Anti-Spam bypass (Nethergames)
        - Flareon = Flareon Anti-Spam bypass (The Hive)
        - GWEN = GWEN Anti-Spam bypass (Mineplex)
        - EDU = Spam on minecraft education edition at mach 10
        - clickgui = all off these in a clickgui that took way too long to make
        
        """
    )
    print("Please Choose one")
    ans = input(">>")
    while True:
        if ans == 'NoBypass':
            modes.BypassesForDiscord.nobypass()
        elif ans == 'Normal':
            modes.BypassesForDiscord.normal()
        elif ans == 'UAC':
            modes.BypassesForMC.UACBypass()
        elif ans == 'Paradox':
            modes.BypassesForMC.ParadoxRIP()
        elif ans == 'Rudedo':
            modes.BypassesForMC.RubedoIsTrash()
        elif ans == 'Radon':
            modes.BypassesForMC.RadonIsHardToBypass()
        elif ans == 'Flareon':
            modes.BypassesForMC.FlareonIsTooGood()
            break
        elif ans == 'GWEN':
           modes.BypassesForMC. MineplexIsGay()
            #this is me now realising how long the ClickGUI is going to take, the last one took 2 hours to make due to glitches... kill me please
        elif ans == 'EDU':
            modes.BypassesForMC.PayForYourGames()
        elif ans == 'FlareonBypass123':
            modes.BypassesForMC.FlareonBeta()
        elif ans == 'clickgui':
            
            print("\nWhat Do You Want Your Message To Be? :)")
            Message = input(">>")
            
            window = tk.CTk()
            window.title("Click\n GUI")
            window.geometry("450x890")

            frame_1 = tk.CTkFrame(master=window)
            frame_1.pack(pady=25, padx=40, fill="both", expand=True)

            nobypass = tk.CTkButton(frame_1, text="No Bypass", command=nobypass2)
            nobypass.pack(pady=25, padx=40, fill="both", expand=True)

            normal = tk.CTkButton(frame_1, text='Normal Mode', command=normal2)
            normal.pack(pady=25, padx=40, fill="both", expand=True)

            uac = tk.CTkButton(frame_1, text='UAC', command=UACBypass2)
            uac.pack(pady=25, padx=40, fill="both", expand=True)

            paradox = tk.CTkButton(frame_1, text='Paradox', command=ParadoxRIP2)
            paradox.pack(pady=25, padx=40, fill="both", expand=True)

            Rudedo = tk.CTkButton(frame_1, text='Rudedo',  command=RubedoIsTrash2)
            Rudedo.pack(pady=25, padx=40, fill="both", expand=True)

            Radon = tk.CTkButton(frame_1, text='Radon', command=RadonIsHardToBypass2)
            Radon.pack(pady=25, padx=40, fill="both", expand=True)

            Flareon = tk.CTkButton(frame_1, text='Flareon', command=FlareonBeta2)
            Flareon.pack(pady=25, padx=40, fill="both", expand=True)

            Gwen = tk.CTkButton(frame_1, text='GWEN', command=MineplexIsGay2)
            Gwen.pack(pady=25, padx=40, fill="both", expand=True)

            edu = tk.CTkButton(frame_1, text='Minecraft Education Edition', command=PayForYourGames2)
            edu.pack(pady=25, padx=40, fill="both", expand=True)

            frame_2 = tk.CTkFrame(master=window)
            frame_2.pack(pady=25, padx=40, fill="both", expand=True)

            appear = tk.CTkButton(frame_2, text='Appearance Settings', command=ldmode)
            appear.pack(pady=25, padx=40, fill="both", expand=True)
        
            window.mainloop()
            
        else:
            print("Unknown Modes!")
            break
else:
    print("\n\n<Flare Spam System> [ERROR] - Unknown Command/Mode/Game!\n! - Games can be found in ./main.py or on startup - !\n\n")
        
